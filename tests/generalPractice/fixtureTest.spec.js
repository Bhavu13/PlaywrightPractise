const { test } = require('../../fixtures/baseFixture');
//const { POManager } = require("../pages/POManager");

test('Verify order creation', async ({ loggedInPage }) => {

   await loggedInPage.click('text=Activity');

});